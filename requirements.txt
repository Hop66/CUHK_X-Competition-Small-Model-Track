# CUHK-X Small Model Track — 核心依赖
# Python 3.10+ / PyTorch 2.x (CUDA)
#
# 安装（服务器已有 conda env `cuhk_x`，此文件用于复现）:
#   pip install -r requirements.txt
# 注: torch/torchvision 建议按 CUDA 版本从 pytorch.org 装，不直接用 pip 默认源

# ============================================================
# 深度学习框架
# ============================================================
torch>=2.1.0
torchvision>=0.16.0

# ============================================================
# 数据处理
# ============================================================
numpy>=1.24.0
pandas>=2.0.0
opencv-python>=4.8.0

# ============================================================
# 权重下载（HuggingFace 镜像）
# ============================================================
huggingface_hub>=0.20.0

# ============================================================
# YOLO 人体检测（生成固定窗口裁剪 bbox）
# ============================================================
ultralytics>=8.0.0
